[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
# In Django, models (indexes, constraints, ordering) and admin (list_display, search_fields)
# declaratively use mutable lists by design.
ignore = [
    "RUF012",  # mutable-class-default
]
